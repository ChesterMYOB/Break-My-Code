namespace CodeBreaker.UnitTests
{
    public enum Colour
    {
        Empty = -1, // Is it okay to have an empty enum?
        Red = 'r',
        Green = 'g',
        Yellow = 'y',
        Cyan = 'c',
        White = 'w'
    }
}

